Propose CANDIDATE causal reasoning only. You do not decide final status, root cause, impact, or CAPA —
those are validated/derived deterministically downstream. Cite claim ids; never invent one.

CASE STATE:
DEVIATION: {observed_deviation}
MECHANISM: {immediate_mechanism} ({immediate_mechanism_status})
CLAIMS (id, claim, status): {evidence_ledger_json}
CONFLICTS: {evidence_conflicts_json}
FINDING (context only, do not restate): {finding_text}

RULES:
1. Verified > Reported. Never call a reported claim verified.
2. Every hypothesis MUST cite supporting_claim_ids and/or contradicting_claim_ids using ONLY the ids
   listed above. If evidence contains reported explanations (e.g. training, workload, discipline), convert EACH into a separate CandidateHypothesis with status="POSSIBLE" citing its claim id.
3. Max 3 hypotheses. Fewer, evidence-grounded ones beat padding the count.
4. If evidence conflicts or contains competing reported explanations, keep hypotheses POSSIBLE and root_cause status="NOT_ESTABLISHED" (statement=null) -- do not pick a side.
5. 5-Why: 1-3 steps, stop with UNKNOWN/MIXED at the evidence boundary.
6. If root cause is not established: status="NOT_ESTABLISHED", statement=null.
7. Every string is ONE short sentence. No markdown, no prose outside the JSON.

Return ONLY this JSON:
{{
  "root_cause": {{
    "status": "NOT_ESTABLISHED",
    "category": "TO_BE_CONFIRMED",
    "statement": null,
    "root_cause_basis": "1 sentence",
    "evidence_required": ["record name"],
    "candidate_hypotheses": [
      {{
        "id": "H1",
        "name": "MECHANISM_TITLE",
        "statement": "1 sentence: the specific plausible cause",
        "supporting_claim_ids": ["C1"],
        "contradicting_claim_ids": [],
        "status": "POSSIBLE",
        "evidence_needed": "concrete record name",
        "confirms_if": "1 sentence",
        "refutes_if": "1 sentence"
      }}
    ],
    "narrative": "1-2 sentences"
  }},
  "five_why": {{
    "steps": [{{"question": "1 sentence", "answer": "1 sentence", "status": "MIXED"}}],
    "is_complete": false,
    "status_note": "short phrase"
  }},
  "contributing_factors": []
}}
