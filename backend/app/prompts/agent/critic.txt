A deterministic check flagged a possible issue in this analysis. Verify it using your own semantic
judgment (not keyword matching) and correct it if real.

FINDING:
{finding_text}

KEY EVIDENCE:
{evidence_ledger_json}

ROOT CAUSE NARRATIVE: {root_cause_narrative}
CANDIDATE HYPOTHESES: {candidate_hypotheses_json}
FLAGGED CONCERN: {flag_reason}

TASK -- DOMAIN RELEVANCE: for the narrative and EACH hypothesis above, does it introduce an actor,
event, or causal domain (training, authorization, communication, supervision, workload, vendor, or
any other) that is NOT actually evidenced in the finding/evidence above? A hypothesis may propose an
explanation the finding doesn't state outright -- the test is whether its SPECIFIC MECHANISM has any
real anchor in what was observed/reported, not whether the words themselves are ordinary. Example to
reject: a missing-calibration-label finding producing a hypothesis about "miscommunication with the
auditor" -- nothing in that finding involves an auditor interaction, so the mechanism is fabricated.

Also flag if evidence grounding is broken elsewhere (an invented entity/number/date, a REPORTED
statement presented as VERIFIED) or if additional investigation would materially improve the
analysis.

Respond with JSON only:
{{
  "approved": true/false,
  "send_back_for_investigation": false,
  "feedback": "one sentence",
  "unsupported_hypothesis_ids": ["id of each hypothesis failing the domain-relevance test, else []"],
  "root_cause_narrative_unsupported": false
}}
Set send_back_for_investigation=true ONLY if additional tool calls would materially improve the analysis.
Set root_cause_narrative_unsupported=true ONLY if the narrative/statement itself (not just a hypothesis)
fabricates a mechanism per the test above.
